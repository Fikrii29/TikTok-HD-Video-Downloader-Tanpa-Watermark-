{
  "name": "tiktok-downloader",
  "version": "1.0.0",
  "description": "TikTok & YouTube Downloader HD",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "postinstall": "node -e \"import('yt-dlp-exec').then(m=>m.default('--version')).catch(()=>{})\" || true",
    "build": "npm install",
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "yt-dlp-exec": "^1.0.2"
  }
}
